import {useEffect} from 'react';


export const Logout = (props: any) => {

    useEffect(() => {
        // props.auth.logout();
    }, [])

    return (
        <>
        </>
    );
}


export default Logout;

